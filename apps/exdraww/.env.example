# Copy to .env.local and adjust. Loaded by Next.js for the browser (NEXT_PUBLIC_*).
# For HTTPS deployments, use https:// and wss:// URLs that the browser can reach.

NEXT_PUBLIC_BACKEND_URL=http://localhost:3001/api/v1
NEXT_PUBLIC_WS_URL=ws://127.0.0.1:8080
